# Tested with the `deploy_mujoco` conda env on Linux + NVIDIA GPU (MUJOCO_GL=glfw).
mujoco>=3.3
numpy>=1.26
scipy>=1.15
imageio>=2.37
imageio-ffmpeg>=0.4
matplotlib>=3.8        # tools/diagnose.py plots
plotly>=6.0           # tools/visualize_graph.py interactive HTML (kaleido optional, for PNG)
